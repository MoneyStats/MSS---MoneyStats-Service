name: Deploy-STG

on:
  push:
    branches: [ develop ]

env:
  JAVA_VERSION: '11'
  MAVEN_OPTS: '-B -DskipTests'
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
  DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
  DOCKER_REPOSITORY: ${{ secrets.DOCKER_REPOSITORY }}

jobs:
  checkout:
    name: 🔧 Checkout
    runs-on: ubuntu-latest
    steps:
      - name: "☁️ Checkout Repository"
        uses: actions/checkout@v3

  setup-java-maven:
    name: 🔧 Setup Java and Maven
    runs-on: ubuntu-latest
    needs: checkout
    steps:
      - name: Set up JDK and Maven
        uses: actions/setup-java@v3
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'adopt'

  install:
    name: 🌱 Install
    runs-on: ubuntu-latest
    needs: [ checkout, setup-java-maven ]
    steps:
      - name: Install dependencies and build
        run: mvn ${{ env.MAVEN_OPTS }} package

  set-release-version:
    name: 📝 Set Release Version
    runs-on: ubuntu-latest
    needs: install
    steps:
      - name: Set Release version env variable
        run: echo "RELEASE_VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)" >> $GITHUB_ENV

  docker-login:
    name: 🐳 Docker Login
    runs-on: ubuntu-latest
    needs: set-release-version
    steps:
      - name: Log in to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ env.DOCKER_PASSWORD }}

  build-and-push-docker:
    name: 🚀 Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: [ set-release-version, docker-login ]
    steps:
      - name: Build & push Docker image
        uses: mr-smithers-excellent/docker-build-push@v6.2
        with:
          image: ${{ env.DOCKER_USERNAME }}/${{ env.DOCKER_REPOSITORY }}
          tags: ${{ env.RELEASE_VERSION }}, latest
          registry: docker.io
          dockerfile: Dockerfile
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ env.DOCKER_PASSWORD }}
          multiPlatform: true
          platform: linux/amd64,linux/arm64
